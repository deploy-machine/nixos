# Per-machine, generated by `nixos-generate-config`. run.sh copies it in on
# first bootstrap; never commit it.
hardware-configuration.nix

# Channel/build noise that may leak into the repo when /etc/nixos is symlinked
# here.
result
result-*
